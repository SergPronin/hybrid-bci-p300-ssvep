# Устранение проблем синхронизации времени LSL

## Проблема

Если вы видите сообщения типа:
```
✗ Маркер плитки 6 пропущен: Недостаточно данных ДО маркера (нужно 0.200 сек до, буфер начинается на -1772374202.741 сек позже)
```

Это означает, что **временные метки маркеров и ЭЭГ данных находятся в разных системах отсчёта времени**.

## Причина

LSL (Lab Streaming Layer) использует единую временную систему для синхронизации всех потоков. Однако некоторые программы (включая некоторые версии Нейроспектра) могут использовать свою собственную временную систему, которая не синхронизирована с LSL.

## Решения

### Решение 1: Настройка Нейроспектра (РЕКОМЕНДУЕТСЯ)

1. **Откройте настройки LSL в Нейроспектре**
   - Найдите раздел "LSL" или "Lab Streaming Layer"
   - Убедитесь, что включена опция **"Использовать LSL время"** или **"Synchronize with LSL clock"**
   - Некоторые версии могут называть это **"Network time synchronization"**

2. **Проверьте настройки времени**
   - Убедитесь, что Нейроспектр использует **LSL clock**, а не системное время
   - Если есть опция "Time source", выберите "LSL" или "Network"

3. **Перезапустите Нейроспектр**
   - После изменения настроек перезапустите программу
   - Убедитесь, что поток ЭЭГ запущен ДО запуска скрипта обработки

### Решение 2: Использование правильного порядка запуска

**Правильный порядок:**
1. Запустите Нейроспектр и начните запись/трансляцию ЭЭГ
2. Подождите 2-3 секунды, чтобы поток стабилизировался
3. Запустите приложение стимуляции (`python -m app.main`)
4. Нажмите START в приложении стимуляции
5. Запустите скрипт обработки (`python scripts/eeg_epoch_processor.py`)

### Решение 3: Проверка синхронизации

Скрипт теперь автоматически проверяет синхронизацию при запуске. Вы должны увидеть:

```
Синхронизация временных меток...
  Ожидание синхронизации маркеров... ✓ (коррекция: 0.000123 сек)
  Ожидание синхронизации ЭЭГ... ✓ (коррекция: 0.000456 сек)
  Разница между потоками: -0.000333 сек
```

**Если разница больше 1 секунды** - это проблема синхронизации!

### Решение 4: Альтернативный подход (если ничего не помогает)

Если синхронизация не работает, можно использовать относительное время:

1. Запустите скрипт обработки ПЕРВЫМ
2. Дождитесь, пока он подключится к потокам
3. ТОЛЬКО ПОТОМ запустите приложение стимуляции
4. Это позволит использовать относительное время от момента подключения

## Диагностика

### Проверка временных меток вручную

Запустите скрипт `lsl_listen.py` и посмотрите на временные метки:

```bash
python scripts/lsl_listen.py
```

Вы должны увидеть что-то вроде:
```
[BCI_StimMarkers] 1234567.890  маркер: 3|on
[EEG_Stream] 1234567.891  получено 10 сэмплов (ЭЭГ)
```

**Временные метки должны быть близки друг к другу** (разница в миллисекундах, а не в часах/годах).

Если вы видите:
```
[BCI_StimMarkers] 1234567.890  маркер: 3|on
[EEG_Stream] 1772374202.741  получено 10 сэмплов (ЭЭГ)
```

Это означает проблему синхронизации - временные метки в разных системах отсчёта.

## Что делать в Нейроспектре

### Для Нейроспектр 4.x / 5.x:

1. Откройте **Настройки** → **LSL** или **Сеть**
2. Найдите опцию **"Синхронизация времени"** или **"Time synchronization"**
3. Убедитесь, что выбрано **"LSL Clock"** или **"Network Time"**
4. НЕ используйте **"System Time"** или **"Local Time"**

### Для других версий:

1. Найдите настройки экспорта/трансляции данных
2. Ищите опции, связанные с временными метками
3. Убедитесь, что используется LSL время, а не системное

## Дополнительная информация

LSL использует единую временную систему для всех потоков в сети. Это позволяет синхронизировать данные от разных источников (ЭЭГ, маркеры, видео и т.д.) с точностью до миллисекунд.

Если ваше оборудование не поддерживает LSL синхронизацию времени, возможно, потребуется:
- Обновить драйверы или программное обеспечение
- Использовать другой способ экспорта данных
- Написать промежуточный скрипт для синхронизации времени

## Контакты

Если проблема не решается, проверьте:
1. Версию Нейроспектра (возможно, нужна более новая версия)
2. Документацию Нейроспектра по настройке LSL
3. Форум поддержки Нейроспектра
