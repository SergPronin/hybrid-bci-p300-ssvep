# Скрипт обработки ЭЭГ данных в реальном времени

## Описание

Скрипт `eeg_epoch_processor.py` предназначен для обработки ЭЭГ данных в реальном времени при проведении экспериментов с P300-SSVEP стимуляцией.

## Установка зависимостей

Перед запуском убедитесь, что установлены все необходимые библиотеки:

```bash
pip install numpy scipy matplotlib
```

Эти библиотеки уже должны быть в `requirements.txt`, но если их нет, установите вручную.

## Как использовать

### 1. Подготовка

Перед запуском скрипта убедитесь, что:

- **Запущено приложение стимуляции** (`python -m app.main`)
- **Нажата кнопка START** в приложении стимуляции (чтобы началась отправка маркеров)
- **Запущен поток ЭЭГ** через LSL (например, Нейроспектр или другое ЭЭГ оборудование)

### 2. Запуск скрипта

```bash
python scripts/eeg_epoch_processor.py
```

### 3. Что происходит

1. **Поиск потоков**: Скрипт автоматически находит потоки маркеров (`BCI_StimMarkers`) и ЭЭГ в сети LSL.

2. **Буферизация данных**: Все входящие данные ЭЭГ сохраняются в буфер на последние 30 секунд.

3. **Извлечение эпох**: При каждом маркере "on" (загорание плитки) извлекается эпоха от -200 мс до +800 мс относительно стимула.

4. **Обработка**:
   - **Фильтрация**: Bandpass фильтр 0.5-10 Гц (убирает шумы и фокусируется на P300)
   - **Baseline correction**: Вычитается среднее значение периода до стимула (-200 до 0 мс)

5. **Накопление**: Эпохи накапливаются отдельно для каждой из 9 плиток.

6. **Визуализация**: Графики обновляются в реальном времени, показывая усреднённые сигналы для каждой плитки.

### 4. Остановка и сохранение

Нажмите **Ctrl+C** для остановки. Скрипт автоматически:
- Сохранит все накопленные эпохи в файл `results/eeg_epochs_YYYYMMDD_HHMMSS.pkl`
- Закроет графики

## Параметры (можно изменить в коде)

- `EPOCH_PRE_STIM = 0.2` - время до стимула (200 мс)
- `EPOCH_POST_STIM = 0.8` - время после стимула (800 мс)
- `FILTER_LOW = 0.5` - нижняя частота фильтра (Гц)
- `FILTER_HIGH = 10.0` - верхняя частота фильтра (Гц)
- `BASELINE_START = -0.2` - начало baseline периода
- `BASELINE_END = 0.0` - конец baseline периода

## Загрузка сохранённых результатов

```python
import pickle
import numpy as np

# Загружаем результаты
with open('results/eeg_epochs_20260301_201530.pkl', 'rb') as f:
    results = pickle.load(f)

# Структура данных:
# results['epochs'] - словарь: tile_id -> список всех эпох
# results['averaged_epochs'] - словарь: tile_id -> усреднённая эпоха
# results['sampling_rate'] - частота дискретизации
# results['num_channels'] - количество каналов
# results['epoch_counts'] - количество эпох по плиткам

# Пример: получить усреднённую эпоху для плитки 0
if 0 in results['averaged_epochs'] and results['averaged_epochs'][0] is not None:
    averaged_epoch = results['averaged_epochs'][0]
    print(f"Форма усреднённой эпохи: {averaged_epoch.shape}")
    # Форма: (num_channels, num_samples)
```

## Понимание результатов

### Что такое эпоха?

Эпоха - это отрезок ЭЭГ сигнала вокруг события (в данном случае - загорание плитки). Мы извлекаем сигнал от -200 мс до +800 мс относительно момента стимула.

### Зачем фильтрация?

Фильтр 0.5-10 Гц:
- **Убирает медленные дрейфы** (< 0.5 Гц) - артефакты от движения, пота и т.д.
- **Убирает высокочастотные шумы** (> 10 Гц) - мышечные артефакты, сеть 50/60 Гц
- **Фокусируется на P300** - компонент P300 находится примерно в диапазоне 0.5-10 Гц

### Что такое baseline correction?

Baseline correction вычитает среднее значение периода ДО стимула из всей эпохи. Это нужно, чтобы:
- Убрать постоянную составляющую сигнала
- Убрать медленные дрейфы
- Сделать сигналы разных эпох сравнимыми

### Почему усреднение?

При усреднении многих эпох:
- **Уменьшается шум** - случайные колебания усредняются
- **Выделяется сигнал** - повторяющиеся паттерны (например, P300) становятся видны
- **Чем больше эпох, тем чище сигнал** - поэтому графики обновляются по мере накопления данных

## Устранение проблем

### Потоки не найдены

- Убедитесь, что приложение стимуляции запущено и нажата кнопка START
- Убедитесь, что ЭЭГ оборудование транслирует данные через LSL
- Проверьте, что потоки доступны в сети (можно использовать `python scripts/lsl_listen.py`)

### Недостаточно данных в буфере

Это нормально в начале работы. Скрипт должен накопить данные в буфере перед первыми маркерами.

### Графики не обновляются

- Убедитесь, что маркеры приходят (проверьте консоль)
- Убедитесь, что данные ЭЭГ поступают
- Попробуйте увеличить `UPDATE_INTERVAL` в коде

### Ошибки фильтрации

Если возникают ошибки при фильтрации, возможно:
- Частота дискретизации слишком низкая (< 20 Гц)
- Параметры фильтра некорректны для данной частоты дискретизации
